# Test Credentials

No application credentials were created or modified during this UI rebuild.

Current blocker for live data routes:
- Missing runtime env values for the Next app: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`
